# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Dropbox
.dropbox
.dropbox.attr
*conflicted copy*

# Editor / tooling
.vscode/
.idea/
*.swp

# Private working material (not for the public repo)
private/
# Root CLAUDE.md only — anchored so example/CLAUDE.md (a public worked example) is not swept up.
/CLAUDE.md

# Dogfooded tracker — kept local + Dropbox only, never pushed to the public repo.
# Deliberately untracked: Dropbox is its version history. tracker-close/supersede
# skip their commit step (see CLAUDE.md standing-rules block).
# Anchored to root so example/tracker/ (a public worked example) is committed normally.
/tracker/
