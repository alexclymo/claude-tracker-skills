# JOURNAL

*File type: **append-only** — entries appended at end of file. Never rewritten.*

Never loaded at session start. Exists purely so that pruning `PRIORITIES.md`'s session window
never means destroying the record — the oldest entry in `## Recent sessions` moves here, verbatim,
the moment a sixth session would push it out.

Entries: `### YYYY-MM-DD — <one line>`, one per session that has rolled off `PRIORITIES.md`.

### 2026-06-18 — Ingest and schema validation (T101, T102) completed and reviewed by both parties
